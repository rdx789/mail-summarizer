# Secrets — never commit these
credentials.json
token.json
.env
cookies.json
medium.com_*.json
medium_cookies.json

# Python
__pycache__/
*.pyc
.venv/
.venv

# Local run state / cache (not secrets, but not meant to be versioned)
last_run.json
article_cache.json

# IDE
.idea/
